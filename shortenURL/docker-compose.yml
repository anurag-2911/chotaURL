version: '3'
services:
  urlshortener:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - 8080:8080

  web:
    build:
      context: ./client  
      dockerfile: Dockerfile
    ports:
      - 3000:80
    depends_on:
      - urlshortener
